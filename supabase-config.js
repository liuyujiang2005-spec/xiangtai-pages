/**
 * Supabase 配置 - 只改这个文件即可
 * 1. 在 Supabase 后台 → Project Settings → API 复制 Project URL 和 anon public key
 * 2. 填到下面两行，保存
 * 3. 在 SQL Editor 运行 supabase-eval-codes.sql
 */
window.SUPABASE_URL = '';      // 例: https://xxxxxxxx.supabase.co
window.SUPABASE_ANON_KEY = ''; // 一长串 anon public key
